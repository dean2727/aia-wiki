[project]
name = "aia-wiki"
version = "0.1.0"
description = "Automated ingestion and update pipeline for aia-wiki."
requires-python = ">=3.13"
dependencies = [
    "feedparser",
    "python-dateutil",
    "pyyaml",
    "requests",
]
